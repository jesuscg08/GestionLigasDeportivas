@model IEnumerable<GestionLigasDeportivas.Models.Equipo>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Liga)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Liga.LigaId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EquipoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EquipoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EquipoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
